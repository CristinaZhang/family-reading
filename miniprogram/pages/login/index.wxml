<view class="container">
  <view class="card">
    <text class="title">家庭阅读记录</text>
    <text class="subtitle">登录以开始使用</text>

    <button
      class="btn primary"
      bindtap="login"
      loading="{{loading}}"
      disabled="{{loading}}"
    >
      微信登录
    </button>

    <button
      class="btn secondary"
      bindtap="devLogin"
      loading="{{loading}}"
      disabled="{{loading}}"
    >
      开发测试登录
    </button>
  </view>
</view>